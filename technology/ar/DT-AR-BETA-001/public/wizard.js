const status=document.querySelector('#status');
document.querySelectorAll('[data-command]').forEach(button=>button.addEventListener('click',async()=>{button.disabled=true;try{const r=await fetch('/api/command',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({type:button.dataset.command})});if(!r.ok)throw new Error('Command rejected');const data=await r.json();status.textContent=`Terkirim: ${data.type} (#${data.sequence})`;status.className='status ok'}catch(e){status.textContent=e.message;status.className='status'}finally{button.disabled=false}}));
